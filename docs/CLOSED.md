# Closed work

Rows closed out of `docs/WORKSTREAMS.md`'s Open work list, in id order. Ids are
stable and never reused; other documents cite them from here.

Kept because a close note is sometimes the grounding a later row needs — what
was agreed, what was tried, what the evidence was. The strikethrough is how
each row was written when it was closed in place; left as-is rather than
rewritten.

| ID | Kind | Where | Item |
|---|---|---|---|
| ~~OW-3~~ | ~~deferral~~ | ~~client workspace input~~ | ~~`setWorkspace` fires per keystroke, so typing an absolute path can enumerate every prefix of it. Decide between debouncing and committing on blur.~~ **Fixed** in `26d104f` as part of OW-39: `setWorkspace` and the per-keystroke server round-trip are gone entirely — the free-form input is replaced by a workspace `<select>` derived from the already-listed sessions, and filtering is now purely client-side over that in-memory list. No keystroke reaches the server, so there is no prefix to enumerate. |
| ~~OW-23~~ | ~~defect~~ | ~~`src/server/composition.ts:14` (`get`), `src/server/sessions/index.ts:82-98` (`listSessions`)~~ | ~~**`SessionIndex.get(ref)` fully parses every session file in both stores on every cold attach.** `get` calls `listSessions()` with no args, which reads and parses the full content of every `.jsonl` file in both stores (line 94) *before* any `cwd` filter is applied (lines 96-98) — so passing a `cwd` filter through, this row's previous fix suggestion, would not help: the filter only trims the array handed to `.find()`, after the expensive walk+parse already happened. `session-manager.ts:286` calls `get` for any session not already in the process table, so every cold attach pays the full-corpus cost — ~0.28s against finding 19's 973-file census, growing with the corpus. **Promoted by D12:** idle/LRU eviction makes cold reattach routine, so this moves onto the hot path — fixing it is a prerequisite to landing OW-33/OW-34, not a follow-up. Real fix, dispatch on `ref.backend` inside `get` instead of delegating to `listSessions`: for Pi, the ref *is* the file path (D9), so `get` should `stat` + `parsePiSession` (`src/server/sessions/pi.ts`) that one file directly, no walk at all. For Codex, the ref's UUID is embedded in the filename (verified against a real file: `~/.codex/sessions/2026/08/12/rollout-2026-08-12T22-10-29-019ff8e2-...-c5.jsonl`) — use `findJsonlFiles` (`src/server/sessions/walk.ts`, a `readdir`-only walk with no file reads) to find the filename match, then `parseCodexSession` only that one file. Done when: a test seeding N session files across both backends, calling `get()` for one Pi ref and one Codex ref, asserts (via a read-call counter or spy on the parse functions) that only the matching file's content is read — not all N — and the existing cold-attach integration test still passes.~~ **Fixed** in `bfc5eda`: `createSessionIndex().get` now delegates to a new `getSession(ref)` in `src/server/sessions/index.ts` that dispatches on `ref.backend` and reads exactly one file — Pi's ref is the path (D9), so a direct `stat` + `parsePiSession` with no discovery at all; Codex's uuid is in the filename, so a `readdir`-only `findJsonlFiles` walk locates the one match and only that file is parsed. `listSessions` itself is untouched. The filename matcher `fileMatchesThreadId` moved out of `sessions/preview.ts` into `sessions/walk.ts`, so the OW-38 preview path and this lookup share one anchoring rule (id as a trailing name segment, so a longer id containing this one does not false-match) rather than two copies; `walk.ts` is the home because `preview.ts` imports `SESSION_ROOTS` from `sessions/index.ts`, so the reverse import would cycle. Pinned by a new `composition.test.ts` case that seeds 6 Pi + 6 Codex files, spies on both parsers, and asserts only the two matching paths were ever handed to one. Confirmed failing first twice — by the executing agent, and again in review by restoring the old whole-corpus `get`, which recorded 12 Pi reads (6 files x 2 `get` calls) against the expected 1. `bun run check` green at 613 tests. Two notes for whoever lands OW-33/OW-34, which this unblocks: `get` no longer verifies that the returned summary's ref equals the requested one (the old `.find()` matched on `sessionKey`; the Codex path now trusts the filename uuid, as `preview` already did), and a Pi ref is read as a path with no check that it sits under the Pi store — both properties the OW-38 preview path established first, on a loopback single-user server. A `findFiles` injection seam the implementation added was removed in review: no test used it. |
| ~~OW-26~~ | ~~defect~~ | ~~`src/client/app.css:112`~~ | ~~**The prompt box is unreachable, which is what makes the app impractical to hand-test.** `.shell` is a `min-height:100vh` grid with no per-pane scrolling, so a growing conversation extends the document rather than scrolling inside itself, pushing the textarea arbitrarily far below the fold. The conversation needs to be its own scroll region with the masthead and prompt pinned. Observed in a browser, 2026-08-12.~~ **Fixed** in `eb57a81`: `.shell` is now a fixed `100vh` grid with the conversation row as `1fr`, and `.conversation` scrolls internally (`min-height:0; overflow-y:auto`) instead of clipping. Verified in a real (Playwright-driven) browser: with 3000px forced into `.conversation`, `document.scrollHeight` stayed pinned to the viewport and the prompt textarea/Send button stayed visible, where before the fix the page grew past the viewport. At the time, this was not pinned by an automated test — no browser test harness existed yet (OW-24). |
| ~~OW-27~~ | ~~defect~~ | ~~`src/client/render/Transcript.svelte`~~ | ~~**No autoscroll.** Agreed spec (2026-08-12): on submit, snap the view so the new message sits near the bottom of the viewport (no-op if there's nothing above it yet), and turn on follow mode. While on, the viewport tracks new content growing below the message, pushing it upward, until the first of: a manual scroll, the message reaching the top of the viewport (where it locks — continuing would push it off-screen), or the turn ending. A jump-to-latest affordance shows whenever scrolled up from the bottom, streaming or not, and snaps once without re-arming follow. Follow-mode scroll adjustments fire on message-boundary upserts, same throttle as the markdown re-render. Each session remembers its own scroll position independently across selection switches; a session never opened defaults to the bottom. Fixing OW-26 is a prerequisite (done, `eb57a81`).~~ **Fixed** in `a9a1e44`/`5432585`: follow mode arms only on submit (`armFollow`), tracks the submitted message as an anchor, and caps `scrollTop` at `Math.min(bottom, anchorTop)` — recomputed every reconcile — so it locks flush at the viewport's top instead of chasing `scrollHeight` past it. A jump-to-latest affordance shows whenever scrolled up (streaming or not) and snaps once without re-arming. Pinned by `App.test.ts`, including a regression for a real defect live-browser testing caught that the jsdom suite missed: `isStreaming` commonly still reads false on the upsert(s) that echo the submitted message and the assistant's own placeholder back (D2's cross-event ordering is not guaranteed), which was disarming follow before the assistant ever started — fixed with a per-anchor `hasStreamed` flag, confirmed failing first. Live-verified end-to-end in a real browser (synthetic SSE over a mocked EventSource/fetch): submit-while-scrolled-away re-arms and snaps near bottom; a long response locks the submitted message flush at the top (measured -0.06px in real layout); manual scroll-up shows jump-to-latest, survives further growth, and clicking it snaps once without re-arming. |
| ~~OW-28~~ | ~~defect~~ | ~~`src/client/App.svelte:115`~~ | ~~**No keyboard submit; only the Send button.** The prompt is a `<textarea>` inside a form, and a textarea does not submit a form on Enter; there is no keydown handler either, so every send is a mouse trip to the button. **Decision: Ctrl-Enter submits** (and Cmd-Enter on macOS); plain Enter inserts a newline, since coding prompts are routinely multi-line. Add a keydown handler on the textarea that calls the existing `submitPrompt` path on Ctrl/Cmd-Enter. Read from source, not yet observed.~~ **Fixed** in `71fbac1`: a keydown handler on the prompt textarea calls the existing submit path on Ctrl-Enter or Cmd-Enter; plain Enter is untouched. Pinned by `App.test.ts`, confirmed failing first. |
| ~~OW-29~~ | ~~defect~~ | ~~`src/client/App.svelte:88`~~ | ~~"New session" is enabled with an empty workspace. Read from source, not yet observed.~~ **Fixed** in `26d104f` as part of OW-39: `New session` now inherits the selected session's workspace and is `disabled` whenever there is none to inherit (`disabled={!newSessionWorkspace}`). Pinned by `App.test.ts` ("disables New session when there is no selected workspace to inherit"). Note: the flip side of this — that a workspace with no sessions can no longer be created at all — is tracked as OW-40. |
| ~~OW-30~~ | ~~defect~~ | ~~`src/client/App.svelte:92`~~ | ~~**Session list is unreadable at scale.** Agreed spec (2026-08-12), modeled on GitHub's AgentsView: each row shows the preview (truncated), workspace, an ISO timestamp to the second (`updatedAt`), a backend badge (pi/codex), and a streaming indicator when `isStreaming`. Stays sorted by recency across all workspaces; narrowing to one workspace is the future workspace-dropdown item's job, not this row's. Distinguishing two sessions in the same workspace relies on the timestamp plus OW-27's per-session scroll memory — no extra affordance needed. No disconnect/close button — D12's idle reaper covers reclamation; the `DELETE` route stays server-side-only, unexposed in the UI. `.sessions` becomes its own scroll region (independent of masthead/conversation/prompt, same treatment `.conversation` got for OW-26), so scrolling a long list never moves the conversation. Two optional additions, both deferrable: a per-row turn-count badge (stretch — D9's listing deliberately reads only each file's header line for cost; a true count needs a full parse per session, which fights that design, so drop it if it does) and a per-row star toggle, purely client-side ephemeral state for visual scanning in a long list — no persistence, no server sync, no effect on sort or filtering.~~ **Fixed** in `44717cc`/`a625cb0`: each row shows backend, workspace, a second-precision UTC timestamp, and a streaming indicator; the preview truncates to a single line (`white-space:nowrap; text-overflow:ellipsis`); the list sorts client-side by recency; `.sessions` gets OW-26's scroll-containment pattern. No close button — an earlier pass added one against the agreed spec, caught in review and removed before landing. Pinned by `App.test.ts`, confirmed failing first; `.sessions` scroll containment verified live (Playwright): with 40 probe rows, `.sessions.scrollHeight` exceeded its `clientHeight` while the page and Send button stayed put. |
| ~~OW-31~~ | ~~defect~~ | ~~`src/client/App.svelte:103`~~ | ~~**Errors are never dismissed.** Agreed spec (2026-08-12): add a manual dismiss (×) on the error banner. Auto-clear-on-next-action is largely already implicit for `view.error` — most `controller.ts` methods already `publish({ error: null })` at the start of the next action — but the displayed `error` is `view.error ?? selectedSession?.error` (`App.svelte:22`), and nothing ever clears a per-session `error` set by an SSE `error` event (`session-state.ts`'s `case "error"`); today only a manual dismiss would ever clear that half. The dismiss control needs to clear whichever of the two is populated.~~ **Fixed** in `1476cf6`: a "Dismiss error" button clears it (`controller.clearError()`), and a session's persisted turn error is now also cleared on the next successful `submit()`. Pinned by `App.test.ts`, `controller.test.ts`, and `session-state.test.ts`, confirmed failing first. |
| ~~OW-36~~ | ~~defect~~ | ~~`src/client/app.css`~~ | ~~**Shell chrome text is oversized relative to the transcript.** `body`/`.shell` never sets a base `font-size`, so `input, select, textarea, button { font: inherit; }` (`app.css:134`) falls through to the browser's unstyled default rather than the design-token scale — `.transcript` is the only place that explicitly opts into `var(--ap-text-md)`. Observed in a browser, 2026-08-12: the Workspace input, Backend select, "New session" button, and every session-list row all read noticeably larger than the conversation, even though their labels (`.session-controls label`) already correctly use `--ap-text-sm`. Fix: set a base `font-size` on `body` (or `.shell`) from the token scale, so the whole shell inherits it by default rather than patching each element individually.~~ **Fixed** in `db1f66c`: `body` now sets `font-size: var(--ap-text-md)`. Verified live (Playwright): computed font-size on the Workspace input and "New session" button before/after. |
| ~~OW-37~~ | ~~defect~~ | ~~`src/server/adapters/codex/adapter.ts:179-189`~~ | ~~**Codex threads start `read-only`, so the agent cannot write outside the workspace (e.g. `~/.cache`) even though sbox mounts it rw.** `thread/start`/`thread/resume` send only `cwd`/`model`/`ephemeral` — no sandbox policy — and app-server defaults a thread to `read-only`. sbox's injected `codex --sandbox danger-full-access` (D7) is a **no-op for `app-server`**: verified against codex-cli 0.147.0, a thread started as `codex --sandbox danger-full-access app-server` still reports `sandbox: {type:"readOnly"}`; only the per-`thread/start` `sandbox` field moves it (`"danger-full-access"` → `dangerFullAccess`, `"workspace-write"` → `workspaceWrite`). Fix: send `sandbox: "danger-full-access"` on both `thread/start` and `thread/resume`. `danger-full-access` is the right value because agentpane already runs Codex inside sbox's bwrap jail (D7) — the OS layer is the confinement boundary, and anything narrower re-implements sbox's mount list in a second place that will drift. The field is `SandboxMode` (`resources/codex-protocol/v2/SandboxMode.ts`, a string enum) on both `ThreadStartParams`/`ThreadResumeParams`; re-export it from `src/server/adapters/codex/protocol.ts` beside the existing param exports. Prefer an adapter-construction option (`sandbox?: SandboxMode`, default `danger-full-access`) mirroring `ephemeral`, since the frozen `StartOptions` (`src/server/adapters/types.ts`) has no field for it. Done when: `adapter.test.ts`'s existing `thread/start` and `thread/resume` params assertions expect `sandbox:"danger-full-access"`, shown red before the change and green after. Corrects OW-18 and the premise in `DESIGN.md`/`HANDOFF.md` that the injected CLI flag governs app-server.~~ **Fixed** in `c2da1cb`: added a `sandbox?: SandboxMode` option to `CodexAdapterOptions` (default `danger-full-access` via `DEFAULT_SANDBOX`), sent as `sandbox: this.sandbox` on both `thread/start` and `thread/resume`; re-exported `SandboxMode` from `protocol.ts`; and corrected the stale `process.ts` comment that described the injected CLI `--sandbox` flag as effective for app-server. Pinned by `adapter.test.ts`'s `thread/start` and `thread/resume` params assertions, confirmed failing first (the two `toEqual`s failed on the missing `sandbox` key before the adapter change, pass after); `bun run check` green (583 tests). Root-cause flag no-op verified against codex-cli 0.147.0. Not yet confirmed live end-to-end that a real Codex turn can now write to `~/.cache` from inside sbox — the fix is proven at the protocol layer, not yet in a live run. |
| ~~OW-38~~ | ~~defect~~ | ~~`src/shared/protocol.ts`, `src/server/http/app.ts`, `src/server/sessions/pi.ts`, `src/server/sessions/codex.ts`, `src/server/sessions/index.ts`~~ | ~~**Selecting an existing session should be able to load a read-only transcript preview without attaching or spawning a subprocess.** Today the only per-session read path is `GET /api/sessions/:backend/:id`, and the contract explicitly defines it as "open this session": `sessionRoute()` calls `sessions.attach(ref)` before replying. Add a distinct non-attaching preview path for stored sessions that reads the backend's JSONL on disk by ref and returns a simplified conversation for the main pane. Keep the scope intentionally narrow: text turns only, flattened for read-only display; skip tools, thinking, images, approvals, and any claim of live parity. Preserve D9's cheap-selection goal by reading **one session file**, not the whole corpus: for Pi the ref is already the JSONL path; for Codex find the matching file by thread id/filename rather than `list().find()`, reusing OW-23's constraint that a targeted read must not parse every session in both stores. Done when: failing-first server tests prove the preview route returns text for representative Pi/Codex stored sessions, does not call `attach()`/spawn, and leaves the existing attach route's spawn-on-open behavior intact.~~ **Fixed** in `a51b085`: new `GET /api/sessions/:backend/:id/preview` route (`SessionPreviewResponse` = `{ ref, turns: { role:"user"|"assistant", text }[] }`) reads exactly one stored file by ref and flattens its text turns, spawning nothing — it goes through a new `SessionIndex.preview()` seam (`deps.index.preview`) and never touches the `SessionManager`. Pi reads the ref path directly (D9); Codex finds the one filename carrying the thread uuid via `findJsonlFiles` (readdir-only), anchoring the id as the trailing name segment so a longer id containing it as a substring does not false-match, then reads only that file. Text turns only — tools/thinking/images/approvals dropped; user-turn synthetic wrappers (`<environment_context>` etc.) filtered via `isSyntheticBlock`. Pinned by `preview.test.ts` (unit, incl. "reads only the matching file" via a counter seam), `app.test.ts` (route never attaches — `attach` spy + `pi.created`/`codex.created` empty — and attach-on-GET still spawns), and `composition.test.ts` (real filesystem walk end-to-end). Fail-first confirmed by the executing agent and re-confirmed in review: injecting `attach` into the route turned the 5 app preview tests red; a read-all-files variant turned "reads only the matching file" red. Extractors additionally verified in review against real `~/.pi` (11 alternating turns) and `~/.codex` (real user turn, wrappers filtered) store files. `bun run check` green (598 tests). The recorded `resources/fixtures/*` are RPC-stream captures, not the on-disk store format these parsers read, so tests synthesize store-format JSONL inline, matching the sibling parser tests. Client wiring is OW-39, not this row. |
| ~~OW-39~~ | ~~defect~~ | ~~`src/client/App.svelte`, `src/client/controller.ts`, `src/client/api.ts`~~ | ~~**Split workspace browsing from session attach/create.** The current free-form `Workspace` box does two jobs at once: it filters the list (`setWorkspace` on every keystroke, OW-3) and it supplies `create(cwd, backend)` for `New session`; selecting a row also attaches immediately, so merely browsing sessions starts agents. Replace that with a recency-sorted workspace dropdown, default `ALL`, derived from the listed sessions; the backend dropdown does not participate in filtering. Selection becomes cheap: on startup and whenever the workspace filter changes, auto-select the most recent session in scope; clicking an existing row loads OW-38's read-only preview immediately, without attach. In preview state, replace the composer with a full-width `Attach` button; clicking it uses the existing attach path, swaps in the prompt box, and focuses it. `New session` should inherit the currently selected session's workspace and remain an immediately promptable new-session flow — do not create a preview-only intermediate state for new sessions. This is the actual product issue behind OW-3, OW-29, and the "future workspace-dropdown item" note in closed OW-30. Done when: failing-first client tests show row selection no longer calls `attach()` for stored sessions, startup/filter changes reselect the most recent in scope, `New session` uses the selected session's workspace, and clicking `Attach` opens the live session and leaves the prompt focused.~~ **Fixed** in `26d104f`, entirely in the client. `api.ts` gains `preview(ref)` (GET `ROUTES.preview`, no body). `controller.ts` adds a `preview: { ref, turns } | null` field to `ControllerView` and a `controller.preview(ref)` that loads OW-38's read-only preview for a stored session (or reselects a session this client already holds a live view for, no re-fetch); `applyAttached` nulls the preview on an explicit attach; `setWorkspace` and the `workspace` var are removed and `refreshSessions` always lists the whole corpus (retiring OW-3 at the source). `App.svelte` replaces the free-form input with a workspace `<select>` (default "All workspaces", sentinel `__all__`) derived from listed sessions, filters the list client-side, auto-selects the most recent in scope on startup and on filter change (via `controller.preview`, so no spawn), renders preview turns as plain role-labelled `Markdown` (read-only, no live-parity claim), and swaps the composer for a full-width `Attach` button in preview state that runs the existing attach path then focuses the prompt. `New session` inherits the selected session's cwd and is disabled with none (closes OW-29). Pinned by failing-first tests in `api.test.ts`, `controller.test.ts`, and `App.test.ts`: row click previews and does not `attach()`; startup and filter-change reselect the most recent in scope; `New session` uses the selected workspace; `Attach` opens the live session and leaves the prompt focused. Fail-first re-confirmed in review — reverting the row handler to `controller.select` turned exactly the "previews instead of attaching" test red and only that one. `bun run check` green (605 tests). Subsumes OW-3 and OW-29; the new-workspace/empty-corpus gap this design creates is filed as OW-40. No live browser run (OW-24). |
| ~~OW-41~~ | ~~defect~~ | ~~`src/client/App.svelte:361-371`, `src/client/controller.ts:219-240`~~ | ~~**OW-39's startup auto-select infinite-loops on a real page load** (`effect_update_depth_exceeded`); the app hangs on load whenever a stored session exists. The auto-select `$effect` (App.svelte:362) calls `controller.preview(top.ref)` while `view.state.selected === null`; the real `preview` (controller.ts:219) publishes synchronously (`publish({ error: null })`, line 227) *before* it awaits `api.preview`, which reassigns App's `view` and re-invalidates the effect before the async resolve flips `selected` — so the effect re-fires on its own publish, synchronously, until Svelte's depth guard throws. The `workspace !== autoWorkspace` guard doesn't converge: the `else if (selected === null && top)` branch (line 368) re-fires on every publish. **Why 605 green tests missed it:** `FakeController.preview` (`App.test.ts:100`) records the call but never publishes, so the real re-entrancy can't occur under test — the fake diverges from the real controller exactly on the looping behavior, and OW-24 (no browser/integration test through the real controller) left it unobserved until a live load. Intent: make auto-select converge synchronously (e.g. track the ref already auto-previewed, not just the workspace, so the guard clears before any re-run) while keeping OW-39's behavior — auto-open the most recent in scope on startup and on filter change, no spawn. Done when: a failing-first test reproduces the loop against a controller/api that publishes like the real one (or renders `App` on the real `createController` + a fake `api`) — throwing/looping before the fix, green after — and App's existing OW-39 auto-select tests still pass. Regression in closed OW-39 (`docs/CLOSED.md`); ids are not reused.~~ **Fixed** in `a06bb51`. The auto-select `$effect` now tracks `autoPreviewedKey` — the `sessionKey` of the ref it already auto-previewed — alongside `autoWorkspace`, and writes that guard *before* calling `controller.preview`. The re-entrant run triggered by `preview`'s own synchronous publish then finds the guard already satisfied and stops, so the effect reaches a fixed point without waiting on the await. `controller.ts` was left unchanged. Evidence: the new test `auto-selects exactly once when preview publishes before it resolves` renders `App` against `PublishingPreviewController`, a fake whose `preview` publishes synchronously with `selected` still null and resolves a microtask later — run against the pre-fix `App.svelte` it fails with `Svelte error: effect_update_depth_exceeded`, the same error the live page load produced, after spinning for 17s; green after the fix. Both OW-39 auto-select tests pass unchanged. `bun run check` green at 606 tests, up from 605. What this left open is OW-42: the fix closed the fake-vs-real publish gap for `preview` only, and deliberately did not touch `controller.preview`'s early `publish({ error: null })`. Behavior narrowing worth knowing: with a per-ref guard, auto-select will not re-fire for the same top-of-list ref within one workspace scope if `selected` returns to null. |
| ~~OW-44~~ | ~~defect~~ | ~~`src/client/App.svelte:441-483`, `src/client/app.css:136-158`~~ | ~~**Session list and workspace dropdown are cluttered: show the directory name instead of the full `cwd` path, put backend + timestamp on one line, and color-code the backend badge.** Four changes, all in the same two files: (1) The `.session-cwd` span (`App.svelte:472`, styled `app.css:157`) renders `summary.cwd` in full; show just the final path segment instead (`cwd` is always an absolute path per the `ALL_WORKSPACES` sentinel comment at `App.svelte:19-22`, so a simple basename split is enough — no trailing-slash handling needed since these are session working directories, not user-typed paths), and add a `title={summary.cwd}` on the span so the full path is still available on hover. (2) Same basename treatment for the workspace `<select>`'s `<option>` text at `App.svelte:443-445` (`{cwd}` -> basename), keeping `value={cwd}` as the full path so filtering (`App.svelte:87`) is untouched; add `title={cwd}` on the `<option>` too. (3) `.session-meta` (`App.svelte:469-482`, `app.css:156`) already renders backend, cwd, timestamp, and streaming dot as flex children in source order — reorder so backend and timestamp are adjacent (cwd can stay between or move after; the ask is backend+timestamp together, not a full relayout) — `flex-wrap` is already there so this is a markup reorder, not new CSS. (4) Color the `.session-backend` span (`App.svelte:470`) by `summary.ref.backend`: green for `codex`, blue for `pi`, grey for anything else. `BackendId` (`src/shared/protocol.ts:20`) is `"pi" | "codex"` today but is a union, not closed to the codebase — implement this as a lookup keyed by backend id with an explicit fallback branch (e.g. a `Record<string, string>`-style map plus `?? var(--ap-fg-subtle)`, not an `if/else` chain that silently mishandles a third id), so a future backend id that nobody updates this for renders grey rather than falling through to whatever `if/else` branch happens to catch it. Reuse existing tokens rather than inventing new colors: `--ap-success` (green) and `--ap-accent` (blue) are already defined with light/dark variants (`app.css:44-47`, `79-82`); `--ap-fg-subtle` is the existing grey. Done when: a client test asserts the session list and workspace dropdown render a basename (not the full path) with the full path as `title`, and the backend badge gets a distinct color per backend plus the grey fallback for an unrecognized id.~~ **Fixed** in `9b478d2`: `basename()` renders the cwd's final path segment in both the session list (`.session-cwd`) and the workspace `<option>`, each carrying the full path as `title`; the option's `value` stays the full path, so the client-side workspace filter is untouched. `.session-meta` markup is reordered so the backend badge and timestamp are adjacent (cwd follows, streaming dot last), no CSS change. The badge's color comes from a `Partial<Record<BackendId, string>>` lookup — `codex` -> `var(--ap-success)`, `pi` -> `var(--ap-accent)` — with `?? var(--ap-fg-subtle)`, so an unrecognised future backend id renders grey rather than matching an if/else branch. Pinned by two new `App.test.ts` tests, confirmed failing first by stashing only `App.svelte` and rerunning (the basename test failed on the missing text, the badge test on an empty `style.color`); in review the badge test was tightened from "three distinct non-empty colors" (which a pi/codex swap would have passed, and which indexed badges positionally against a recency sort) to asserting each backend's exact token by badge text. `bun run check` green at 609 tests. |
| ~~OW-45~~ | ~~defect~~ | ~~`src/client/App.svelte:438-456`, `src/client/app.css:136-148`~~ | ~~**Put "New session" on the same line as the Backend selector, and shrink its label.** `.session-controls` (`App.svelte:438-456`) is a single-column grid (`app.css:136`: `display: grid; gap: var(--ap-space-3)`), so the Workspace `<label>`, Backend `<label>`, and the `New session` `<button>` (`App.svelte:455`) each stack on their own line. Wrap the Backend label and the button in a row container (flex, `align-items: flex-end` so the button lines up with the `<select>`'s baseline rather than the "Backend" text label above it) so they share one line; leave Workspace on its own line above. Shrink the button's label from "New session" to "New" (or a "+" glyph with an `aria-label="New session"` if going iconic — either satisfies the ask, pick one) — it doesn't need to be a wide button once it's sitting next to the selector rather than spanning the sidebar. `createSession`/`disabled={!newSessionWorkspace}` (`App.svelte:397-399`, `455`) are unchanged; this is layout and label text only. Done when: a client test asserts the button's accessible name is "New" (or "New session" via `aria-label` if iconic) and it renders inside the same row element as the Backend `<select>`.~~ **Fixed** in `c9798aa`: a `.session-controls-row` flex container (`display:flex; align-items:flex-end`, with `flex:1` on its label so the select takes the remaining width) wraps the Backend label and the button so they share one line; Workspace keeps its own line above. The label is now "New" rather than an icon, so the accessible name is "New" — the two existing tests that queried the button by its old name were updated to match, asserting the same things. Pinned by a new `App.test.ts` test that the button and the Backend `<select>` share a row container, confirmed failing first (it could not find a button named "New" against the pre-change markup). `bun run check` green at 609 tests. Note for whoever reads the diff: the executing worktree was cut two commits behind `main` and so did not contain OW-44; the changed regions did not overlap and the patch applied cleanly, with `bun run check` re-run on `main` before the commit. |
| ~~OW-46~~ | ~~defect~~ | ~~`src/client/App.svelte:459-460`, `src/client/session-state.ts:85-105`~~ | ~~**A freshly-created session's sidebar entry keeps its placeholder label after the first prompt and never catches up without a full page reload.** Reported live: create a session, submit the first prompt, and the row keeps showing the raw ref; reloading the browser is what finally shows real text. The label is `summary.preview \|\| ` `${backend} ${id}` ` (`App.svelte:460`), and `summary.preview` is server-supplied — `parsePiSession` fills it from the **first** user text message in the stored JSONL and `break`s (`pi.ts:69-94`). A new session has no such file until the first prompt materialises it, and the only re-list tied to that moment is the single `sessionsChanged()` fired inside `#adoptRef` (`session-manager.ts:270`), which the client answers with exactly one `refreshSessions()`. Nothing re-triggers another list for this session — no event fires on turn completion, only on create/attach/rename/close (`session-manager.ts:185,218,270,386,445`). So if that one fetch reads the file before Pi has flushed the user turn, `preview` stays `null` with no second chance. **Fix at render, and do not diagnose the race:** when `summary.preview` is null, fall back to the first `role:"user"` message's text from the live view the client already holds (`view.state.sessions[sessionKey(summary.ref)].messages`) before falling back to the ref. Three things make this the cheap correct fix, all checked at the source: (a) it needs no extra fetch and no new broadcast — a `sessionsChanged()` on turn end would cost a full-corpus walk+parse per turn (OW-23); (b) it is immune to the write-ordering question entirely, because the client sent that text and holds it in memory with certainty, whatever the file has; (c) first-user-message is *exactly* the server's own preview semantics (`pi.ts:82-88`), so the fallback and the server value that eventually arrives agree rather than flickering between two different strings. Safe across the rename because the `"renamed"` reducer re-keys `state.sessions` from the virtual key to the real one (`session-state.ts:96-98`), so `sessionKey(summary.ref)` still finds the view afterward. No truncation logic needed — `.session-preview` already ellipsizes (`app.css:155`). Deliberately **not** covered, and not defects to chase from this row: a session created in another browser tab still shows a stale label until something re-lists it, and whether Pi flushes before `submit()` resolves stays unanswered — this fix makes that question stop mattering for the observed symptom. Done when: a failing-first client test drives `App` through the real SSE order (`sessions-changed`, `renamed`, `snapshot` — `broadcaster.ts:137-147`) with a summary whose `preview` is `null` and a snapshot carrying a user message, asserting the sidebar row shows that message's text rather than the ref; and the existing behavior for a session with no messages and no preview — the ` `${backend} ${id}` ` fallback — still holds.~~ **Fixed** in `4ef9c72`, at render as the row directed: `sessionLabel(summary)` in `App.svelte` returns `summary.preview`, else `firstUserText(summary)` — the first `role:"user"` message in `view.state.sessions[sessionKey(summary.ref)]`, non-empty `type:"text"` blocks joined on a space and trimmed — else the `${backend} ${id}` ref. Being a plain function called from the template it re-reads `view` reactively, so the row upgrades to the server preview the moment one arrives. Verified in review that the fallback matches `parsePiSession`'s semantics exactly (`pi.ts:83-88`: same filter, same `" "` join, same trim), differing only by `trimPreview`'s truncation, which the row deliberately left to `.session-preview`'s existing ellipsis; Pi's parser applies no synthetic-block filtering (only `codex.ts` has `isSyntheticBlock`), so there is nothing there for the fallback to mirror. Pinned by a new `App.test.ts` case that replays `sessions-changed` -> `renamed` -> `snapshot` through the real `reduceServerEvent` rather than hand-assembling the end state, with `preview: null` on the summary. Confirmed failing first: the pre-fix component could not be found by the row's accessible name and the printed DOM showed `pi /home/u/.pi/sessions/abc.jsonl`. The `${backend} ${id}` fallback for a session with no messages and no preview still passes unchanged. `bun run check` green at 610 tests. |
| ~~OW-43~~ | ~~defect~~ | ~~`src/server/sessions/pi.ts:122`, `src/server/sessions/codex.ts:196`, `src/server/sessions/line-reader.ts:31-32`~~ | ~~**OW-38's preview extractors inherit `readLinesLfOnly`'s enumeration-sized caps, so the preview silently ends well before the real end of a long session.** `extractPiPreviewTurns` and `extractCodexPreviewTurns` both call `readLinesLfOnly(filePath)` with no options, so they get `DEFAULT_MAX_LINES = 200` / `DEFAULT_MAX_BYTES = 512 * 1024` (`line-reader.ts:31-32`) — caps sized for `parsePiSession`/`parseCodexSession`'s enumeration path, which reads just far enough to find one message and `break`s (D9's cheap-listing goal). The preview extractors don't `break`; they iterate to (what should be) the end of the file, so on any session past 200 lines or 512KB they just stop mid-file with no error and no signal — fewer turns than the session actually has. Attaching doesn't share this cap and shows everything, which is the discrepancy reported live: the preview pane ends early, hitting Attach shows the whole transcript once the subprocess is up. No existing test catches this — `line-reader.test.ts` pins the caps directly, and `preview.test.ts` (222 lines) has no fixture anywhere near 200 lines / 512KB. Fix: give the preview extractors their own unbounded (or much larger, with truncation surfaced to the client rather than silent) read, distinct from the enumeration callers' bounded one — `readLinesLfOnly` already takes `maxLines`/`maxBytes` as options, so this is plumbing, not a rewrite. Done when: a failing-first test writes a stored session past both current defaults and asserts the preview contains turns from past line 200 / byte 512KB, and the existing enumeration tests (which rely on the bounded read staying cheap) still pass.~~ **Fixed** in `acb0060`: `extractPiPreviewTurns` and `extractCodexPreviewTurns` now pass `{ maxLines: Infinity, maxBytes: Infinity }` to `readLinesLfOnly`; the enumeration callers keep the no-args bounded read and their cost assertions unchanged. Unbounded was chosen over "larger, with truncation surfaced" deliberately — attaching already shows the whole transcript, and closing that discrepancy is the row's entire point, so no protocol field and no truncation signal were added. Pinned by one new `preview.test.ts` case per backend, each synthesizing a 301-line / ~900KB store-format fixture (300 alternating turns of ~3KB) and asserting all 300 turns return, including `turns[250]`, which sits past both caps. Confirmed failing first with the extractor change stashed out: both failed `expected 167 to be 300` — the 512KB byte cap biting first, at roughly line 167. `bun run check` green at 612 tests. The executing worktree was cut behind `main`; the patch applied cleanly and `bun run check` was re-run on `main`, where review also corrected the two test names, which cited OW-47 instead of this row. |
| ~~OW-47~~ | ~~defect~~ | ~~`src/client/App.svelte:61,178-187,213-260,312-352`, `src/client/App.test.ts:172-200`~~ | ~~**Autoscroll degrades over a long conversation until it stops entirely — cause unknown, reproduce before fixing.** Reported live 2026-08-14, with a clear progression *within one conversation*: early turns behave exactly to the OW-27 spec (the submitted prompt rises until it reaches the top of the pane, where autoscroll stops); later turns stop earlier and earlier with the prompt still mid-pane; eventually submitting only appends the message at the bottom of the panel and nothing scrolls at all while the answer streams. **Reloading does not help; starting a new conversation does**, in the same page session. Autoscroll arms on submit (`armFollow`), and stops on exactly the three conditions of the agreed spec (OW-27): a manual scroll, the prompt reaching the pane top, or the turn ending. The symptom means one of the three fires early — **this row is to find out which, not to pick one.** Three candidates, none confirmed: (a) *phantom manual scroll* from two programmatic scrolls racing the boolean `suppressScrollHandling`; (b) *premature turn-end* from an `isStreaming` false mid-stream; (c) *anchor lost*, `reconcile` silently scrolling nothing when `querySelector` misses. Reproduction vehicle: OW-27's close note records live-browser verification via synthetic SSE, but no such harness is in the repo — rebuild it, and commit it this time. Done when: the cause is identified from logged evidence in a real browser, recorded in `MANUAL_TESTING.md`; a failing-first test pins it; and a long conversation autoscrolls on every turn the way its first few do.~~ **Fixed** in `20470d3`, with a one-declaration change: `overflow-anchor: none` on `.conversation` (`src/client/app.css`). **The cause was none of the three candidates.** It is **CSS scroll anchoring** — the browser repositioning `scrollTop` itself to hold a visual anchor steady as content reflows above the viewport, under-compensating (~10px per adjustment, instrumented) as it does. That fires a `scroll` event the application never performed, and `handleConversationScroll` cannot distinguish it from a reader grabbing the scrollbar, so it clears the anchor and follow disengages mid-turn. Candidate (a) was the right *shape* — a phantom scroll read as manual — but the wrong source: not two programmatic scrolls racing one boolean, but the browser scrolling on its own. Two anchoring mechanisms on one scroller fight; follow mode already does this job against a message it picked, so the browser is told to stop doing it too. **A guard against the boolean race was written, measured to change no outcome, and reverted** rather than landed speculatively — that race remains real but unproven, filed as OW-48. Evidence in `MANUAL_TESTING.md` ("Observed follow-mode decay in a real browser"): headless Chromium 151, all 10 turns of a 40-turn-deep conversation lock flush at the pane top (≤2px) with the fix; with it reverted the first measured turn strands **51px** down. Fail-first re-confirmed independently in review by `sed`-ing the declaration out and re-running. **Reproducing it requires a turn shape that *shrinks* mid-turn** — `Thinking.svelte` holds its `<details>` open only while it is the streaming block, so a landing `toolCall` collapses it and the transcript gets shorter; text-only turns were driven to 200 seeded turns and never reproduced it. Any future follow-mode harness needs a contracting turn, not just a growing one. Lands the repo's **first browser vehicle** (`e2e/`, Playwright, `bun run test:browser`): the real `App.svelte` and real `controller.ts` against a synthetic `AgentpaneApi` port, no backend and no subprocess. jsdom has no layout, no scroll anchoring and no real scroll-event timing, so it could not have discovered this and cannot pin it. Deliberately one defect's harness, **not** the E2E suite OW-24 still tracks; not wired into `bun run check` because it needs a browser (OW-49). `bun run check` green at 612 tests, unchanged — the pin lives in the browser vehicle. The executing agent was terminated mid-task by a spend limit having reverted the speculative guard; review verified the fix, the fail-first, and `bun run check` on `main`, and wrote the `MANUAL_TESTING.md` and `AGENTS.md` entries it had not reached. |
| ~~OW-50~~ | ~~change~~ | ~~`src/client/App.svelte:545-561`, `src/client/app.css:185-198`, `src/client/render/Message.svelte:71`~~ | ~~**The read-only preview is a second transcript renderer, and it has already drifted from the first.** `app.css:186-197` re-implements `Transcript.svelte`'s container plus `Message.svelte`'s `.msg`/`.user`/`.who` rules with the same tokens and the same values, and two divergences already exist: the preview labels both roles ("You"/"Agent", `App.svelte:551`) where the transcript labels only the user (`Message.svelte:44,52`), and `.transcript` sets font-family, size, line-height, colour and background (`Transcript.svelte:34-44`) where `.preview` sets none of them and inherits. Fix it **client-side only** — the server keeps returning `SessionPreviewTurn[]`. Do *not* widen `SessionPreviewResponse` to carry `AgentMessage`s: that is a D11 frozen-interface change, and its Codex half needs a whole second mapper, because stored rollout records are `response_item` payloads rather than the `ThreadItem`s `mapping.ts` consumes. Instead map each turn to a message-shaped object in the client, render it through `Transcript`, and delete `App.svelte:548-558` and `app.css:186-197`. A synthetic assistant message typechecks without lying about a provider: `Api` and `ProviderId` are open unions, so `provider` and `model` carry the backend id (`pi`/`codex`) — the owner's ask, since that is the one identity a preview genuinely knows — and `api` a placeholder; `stopReason:"stop"` with zero usage suppresses both the error banner and the token line. One consequence to handle: the meta line renders only when `usage.totalTokens > 0` (`Message.svelte:71`), so the backend id would never appear — split that condition. Keep `.preview-empty`'s wording. **Success criterion: the preview and an attached session render the same DOM structure for the same content.**~~ **Fixed** in `f7aa3ae`: a new pure `previewMessages(turns, backend)` (`src/client/preview.ts`) maps `SessionPreviewTurn[]` to `AgentMessage[]` — user turns pass straight through as `{role,content,timestamp}`, assistant turns carry `provider`/`model` = the backend id, `api:"session-preview"`, `stopReason:"stop"` and zero usage — and `App.svelte` renders the result through the same `Transcript` the attached path uses. The bespoke markup and `app.css`'s `.preview`/`.preview-turn`/`.preview-role` rules are gone, the now-unused `Markdown` import with them; `.preview-empty` and its wording stay, rendered *instead of* `Transcript` on zero turns rather than through it, because "No messages yet" would misreport a session whose text-only extraction came up empty. `Message.svelte`'s meta line is split so the model span renders whenever there is a model and the token/cost spans only when there is usage, which also unhides the model for a real turn reporting zero usage. `timestamp` is a constant, not a clock read: it feeds only `keyFor`, which already carries index and role, so a clock read would churn the `{#each}` key on every re-render. Server untouched; `SessionPreviewResponse` unchanged. Pinned by three new tests, all confirmed failing first: the success criterion itself (preview and attached `.transcript` innerHTML identical for the same content — red as `expected undefined to be truthy`, there being no `.transcript` on the preview path at all), the labelling and backend attribution (red on finding `<span class="preview-role">Agent</span>`), and the zero-usage model span (red as `expected '' to contain 'example-model'`). The empty-preview wording test passes trivially against the old code, so it was reproduced red by temporarily routing the empty case through `Transcript`; that is recorded here rather than claimed as a fail-first. **The row's guess that `App.test.ts` asserted on `.preview-role`/`.preview-turn` was wrong** — nothing did, so there was coverage to add and none to update. One incidental consequence, read from the cascade and **not visually confirmed** (OW-55): the deleted global `.preview` rule was also hitting `Thinking.svelte:58-59`'s collapsed summary span, which carries the same class name and sets no `display`/`gap`/`padding` of its own. `bun run check` green at 617 tests. |
| ~~OW-52~~ | ~~change~~ | ~~`src/client/render/Message.svelte:44`~~ | ~~**Drop the visible "You" label from user turns.** The filled surface, accent left border and width cap (`Message.svelte:114-122`) already mark a user turn as the reader's own; the word is redundant. Applies to the preview too, which after OW-50 renders through this same component. **The `<span class="who">` is currently the only role signal for assistive technology** — `data-role` is not announced, the `<article>` carries no `aria-label`, and assistant turns have no cue at all — so removing it leaves the transcript role-less to a screen reader. **The project owner accepted that explicitly on 2026-08-15**, recorded here so a future agent reads it as decided rather than as a bug to helpfully fix. Revisit if anyone but the owner uses this.~~ **Fixed** in `46f2c8a`: the span is gone, and with it the `.who` rule, which existed only for it — grep across `src/` and `e2e/` confirms no other markup or CSS referenced either. Pinned by a new `Transcript.svelte.test.ts` case asserting a user turn renders no `.who` and no "You", confirmed failing first against the unmodified component (`expected <span class="who svelte-sbhkvv">You</span> to be null`). OW-50's preview test was updated rather than deleted: its intent is that the preview labels turns exactly as the transcript does, which now means *neither* role is labelled, so its "You" assertion inverted and its name gained "(no role label)"; its backend-id assertion is untouched. The executing agent proposed opening an `OW-` row for the assistive-technology regression; deliberately **not** opened, because it is not undone work — it is the decision this row records, and the trigger to revisit it ("if anyone but the owner uses this") is stated above. No `aria-label`, `role`, or visually-hidden span was added to compensate. `bun run check` green at 618 tests. |
| ~~OW-51~~ | ~~change~~ | ~~`src/client/render/transcript.ts`, `src/client/App.svelte`~~ | ~~**A "reading view" toggle that elides tool calls, tool results and thinking blocks, for scrolling back through a session while it is still running.** Add a pure `condense(messages)` beside `buildTranscript` (`transcript.ts:45`) that drops `toolResult` messages and `toolCall`/`thinking` blocks and keeps user and assistant text. Keep each entry's *original* index so follow mode's `data-index` anchor still resolves (`App.svelte:219`); the anchor is always a user message (`App.svelte:340`), which condensing never elides, so OW-47's scroll work is untouched. Decisions, all taken with the project owner on 2026-08-15: **the name is "reading"**; **one global boolean, not persisted, not per-session**; **the control goes in `.session-controls`**; **scroll position may jump when toggling** and pinning is not wanted yet; **the toggle stays visible and inert while previewing**; **hiding thinking blocks is a first cut, not a settled decision** — the owner wants to use the feature before deciding. No liveness cue is needed: the Abort button renders only while streaming and the sidebar row carries a ●.~~ **Fixed** in `f0459b6`: `condense(view: TranscriptView): TranscriptView` takes a *built* view rather than raw messages, which is what makes the index constraint hold by construction — filtering entries preserves `TranscriptEntry.index`, where mapping over `messages` and re-deriving position would renumber everything after an elision and strand the follow anchor. `Transcript.svelte` gains a `reading` prop; `App.svelte` gains `let reading = $state(false)` and a `aria-pressed` toggle in `.session-controls`, which needed no CSS — the existing global `button[aria-pressed="true"]` rule already styles it. Nothing in the scroll path was touched. Keys are preserved on surviving entries, so toggling updates the DOM in place rather than rebuilding it. **One decision beyond the row, taken by the executing agent and worth knowing:** assistant turns that condense to zero blocks are dropped (else they render an empty article, or a bare cursor if streaming), **except** a turn whose `stopReason` is `error` or `aborted` — those already carry empty `content` today and their entire payload is the banner, which is not tool chrome, so a blanket drop would have made reading view silently swallow "Aborted." and provider errors. The streaming `pending` placeholder is still dropped, so there is no bare cursor. Both halves are one line and one test to reverse. Pinned by 12 new tests. Fail-first was done in the strong form: rather than one red run, the agent broke `condense` six separate ways and recorded which test caught each — re-deriving index from condensed position (red on `[0,1]` vs expected `[0,3]`, the follow-mode assertion), keeping toolCall/thinking blocks, keeping empty entries, keeping tool results, mutating `message.content` in place, and dropping the `{reading}` prop and the button. `bun run check` green at 630 tests; `bun run test:browser` re-run on `main` after the merge, green (`e2e/follow.spec.ts`, 29.2s). Reviewed and rejected as a blocker: dropping the streaming tail shifts `lastIndex` to an already-finished entry, which then renders with `streaming=true` — but that flag only drives `Markdown`'s frame throttle and the empty-content cursor, and a finished message's text does not change, so there is no visible consequence. Two gaps left open as OW-56. |
| ~~OW-53~~ | ~~defect~~ | ~~`docs/WORKSTREAMS.md:25`~~ | ~~**The client-shell Status cell restates the closed-work list instead of citing it, and grows every time a row lands.** It is now a running narrative of every client defect found since 2026-08-12, naming OW-24, OW-26..OW-31, OW-36, OW-38..OW-47 and OW-49 in prose, with a clause appended per close (see `9ad0a6d`, "record the four landed rows in the status table"). That breaks the rule this file and `AGENTS.md` both state — cite ids, never restate a row — and this cell is where the file previously contradicted itself, claiming "no live Pi" that the paragraph above it had already disproved (the correction is still visible at `WORKSTREAMS.md:47-49`). The fix is not only to rewrite the cell but to **remove the reason to append to it**: a Status cell that carries a state plus the ids still *open* stops growing when a row closes, because closing one shortens it. What was fixed is `docs/CLOSED.md`'s job. Check the other five Status rows for the same drift while there — they are one line each today, which is the target shape. Docs-only, so `bun run check` does not apply; the check that matters is that closing the next row requires no edit to this cell.~~ **Fixed** in `6f07f49`: the cell went from 981 characters to 118, against 56–156 for the other five rows, and now carries a state plus the ids still open for the slice — `(open: OW-24, OW-42, OW-48, OW-49, OW-56)`. The structural property the row asked for holds by construction: closing any of those five means deleting its id from that list and editing nothing else, so the cell shrinks on close instead of growing. It is not yet *demonstrated* — the next client row to close is the demonstration. **Every fact deleted was checked to survive elsewhere before deletion**, not assumed to: the 2026-08-12 hand-run date (six occurrences in this file, plus `WORKSTREAMS.md`'s own Open work preamble), OW-47's browser vehicle deliberately not discharging OW-24 and not being wired into `bun run check` (verbatim in OW-47's close note), OW-26's layout defect making hand-testing impractical, OW-29→OW-39, OW-39→OW-41's infinite loop on load, and `PublishingPreviewController` — all in their own close notes here. **One claim was knowingly not preserved:** the old cell's framing that "a second live run on 2026-08-14 produced OW-43..OW-47" as one group. Individually OW-47 cites 2026-08-14, OW-43/OW-46 say "reported live" with no date, OW-44/OW-45 read as agreed-spec polish rather than live-observed defects, and `MANUAL_TESTING.md:321` records a 2026-08-14 hand-open tied only to OW-41's freeze. The grouping could not be verified at the source, so it was dropped rather than written into a close note as fact; all five rows it concerned are closed, so nothing depends on it. **OW-40 is deliberately not cited** in the new cell: it is a deprioritized deferral, and the other Status rows cite none of theirs either (pi-adapter omits OW-10, OW-15, OW-16). The other five Status rows were checked for the same drift and are already at the target shape — unchanged. Review caught one overreach in the executing agent's draft and reverted it: it had upgraded the cell's state to "**done**, offline verified, …", matching the other five, but client-shell was the one slice the table had never marked done and this row authorized a change of shape, not of state. Docs only, so `bun run check` does not apply and was not run. |
| ~~OW-57~~ | ~~defect~~ | ~~`.claude/skills/execute/SKILL.md:26-33`, `AGENTS.md` "Landing work"~~ | ~~**A dispatched subagent is told to commit somewhere it cannot, so it commits nothing and the parent retypes its diff onto `main` by hand.** Step 2 sends it into its own worktree and opens by telling it to read `CLAUDE.md`, which leads to `AGENTS.md`: *"Commit directly on `main` as the work is done... No branch, no PR."* That instruction is unexecutable from a worktree checked out on a branch that is not `main`, so the safe reading is to leave everything uncommitted — which is what happens. The branches never advance past their starting commit, and step 4 "Land" is really the parent re-applying a diff: `git log --merges` shows nothing since the `OW-` era began, and the close notes say it outright (OW-43, OW-45: *"the patch applied cleanly and `bun run check` was re-run on `main`"*). Nine worktrees accumulated in this checkout before being removed by hand at the owner's request, every one of them holding work that had already landed by other means. Fix both halves: give the dispatch prompt an explicit instruction to commit on its own branch, and scope `AGENTS.md`'s rule so it reads as addressed to the session agent rather than to everything that loads it — otherwise the next dispatched agent hits the same contradiction. Then step 4 becomes `git cherry-pick`, `git commit --amend` for whatever review changed, `bun run check`, `git worktree remove`, `git branch -D`. **Load-bearing:** the amend, because step 3 review deletes out-of-scope work and a bare cherry-pick would land the unreviewed version; and cherry-pick rather than `git merge`, because history on `main` is linear and a merge commit per row breaks that. **Incidental:** whether the worktree is torn down before or after the close commit. Note this does not by itself fix OW-58 — it converts a stale worktree from a paragraph of explanation into a cherry-pick conflict, which is an improvement but a different one. Done when an execute session lands a row this way and its close note carries the cherry-picked sha, with `git worktree list` and `git branch` each showing only `main` when the session ends. Raised by a second agent reviewing the workflow, 2026-08-16.~~ **Fixed** in `9b786a6`, and the fix was exercised by the session that landed it. The dispatched agent was told in its prompt that it commits on its worktree's branch and cannot commit on `main`; it committed `147d3f3` on `worktree-agent-a3c851d743d310398`, and that commit was cherry-picked onto `main` as `9b786a6` — the first dispatched work in the `OW-` era to reach `main` as its author's own commit rather than as a retyped diff. `git worktree list` and `git branch` each show only `main`. **The amend was not exercised:** review found nothing out of scope and nothing to change, so `git commit --amend` had no work to do; step 4's amend clause is written but still unproven, and the first row whose review deletes something is the test of it. `bun run check` was not run — both files are docs, and `AGENTS.md` exempts docs-only commits. **One observation this run produced, which belongs to OW-58 and is deliberately not acted on here:** the dispatched worktree was cut 19 commits behind `main` (`9ad0a6d`, against a tip of `c04f26c`) and the agent fast-forwarded it by hand before editing, which is why the `AGENTS.md` line numbers it was given were off by six. That is a third instance of the staleness OW-58 records, and a much larger one than the two it cites; recording it as live-run evidence in `MANUAL_TESTING.md` is OW-58's job, not this row's. |
| ~~OW-58~~ | ~~defect~~ | ~~`.claude/skills/execute/SKILL.md:26-28`, OW-43 and OW-45 close notes in `docs/CLOSED.md`~~ | ~~**Dispatch has no freshness step, and worktrees have twice been cut behind `main`.** OW-45's close note says the worktree was *"cut two commits behind `main`"*, OW-43's that it was *"cut behind `main`"*; both were handled by re-running `bun run check` on `main` and explaining the gap in prose, which is bookkeeping standing in for a fix. **Verify the mechanism before designing one:** the Agent tool's `isolation: "worktree"` does not document which commit it branches from, and nobody has checked whether it uses the parent session's start HEAD, the repo HEAD at spawn, or something else. The answer can invert the fix — if it already cuts from HEAD at spawn, then the staleness came from elsewhere (the parent session holding a stale HEAD is the obvious candidate) and adding a rebase at dispatch would be treating a symptom while the cause keeps producing new ones. Cheapest measurement: land a commit, then dispatch a throwaway worktree subagent that reports `git rev-parse HEAD` and nothing else, and compare it to `main`'s tip. Done when that comparison is recorded in `MANUAL_TESTING.md` as live-run evidence, and *either* the dispatch step gains an explicit cut-from-current-tip instruction *or* this row closes on the measurement showing the harness already does it and naming what actually caused the two stale runs. Lower urgency if OW-57 lands first, since staleness stops being silent, but not closed by it. Raised by a second agent reviewing the workflow, 2026-08-16.~~ **Fixed** in `504e0cb`; the evidence is `MANUAL_TESTING.md`'s "Observed worktree base for dispatched subagents". **The row's two candidate mechanisms were both wrong.** It is neither the parent session's start HEAD nor the repo HEAD at spawn: the harness cuts from the **remote tracking ref**. Two dispatched worktrees in one session both started at `9ad0a6d`, which is where `origin/main` and `origin/HEAD` point, while local `main` was at `c04f26c` and then `cda74e6` — the same base across two runs `main` had moved between is what identifies it, and the branch reflog states it outright (`branch: Created from origin/main`). Because `AGENTS.md` forbids `git push` unless asked by name, `origin/main` is frozen at the last push and the gap grows without bound: 19 commits for the run that landed `9b786a6`, 21 for the run that recorded this. **That is what caused the two stale runs OW-43 and OW-45 recorded** — no stale parent HEAD was involved, and neither close note needs revisiting. Of the row's either/or, the first branch was taken: dispatch now requires the prompt to tell the subagent to `git merge --ff-only main` before starting and to report the sha it started at, so a stale base surfaces in the report instead of in wrong line numbers. Keeping `origin/main` fresh by pushing would fix it at the source and was rejected — the never-push rule. **This run exercised step 4's amend**, which OW-57 left unproven: review found the record argued the base circumstantially from matching shas when the reflog states the mechanism directly, so the reflog line and the command that produces it were added on top of the subagent's `5edccfe`, landing as `504e0cb`. `git worktree list` and `git branch` show only `main`. Docs only, so `bun run check` does not apply and was not run. |
| ~~OW-61~~ | ~~defect~~ | ~~`src/client/render/Message.svelte`, `src/server/adapters/codex/adapter.ts`, `src/server/adapters/codex/reducer.ts`, `src/server/adapters/types.ts`, `resources/codex-protocol/v2/Thread{Start,Resume}Response.ts`~~ | ~~**Assistant-turn metadata shows model but drops Codex reasoning effort, so the transcript cannot display the effort level beside the model name.** The agreed scope is deliberately narrow: when effort is present, render it alongside the model in the assistant-turn meta; when absent, omit it with no placeholder. Do not chase mid-session settings churn — this UI does not let the owner change model or effort, so capturing the value on attach/start is enough. Pi's RPC protocol exposes no corresponding field, so this is expected to be Codex-only.~~ **Fixed** in `72f744f`. The row's paths held up; `src/server/adapters/types.ts` needed no change and was not touched. The type problem the row did not name: `AssistantMessage` comes from `@earendil-works/pi-ai`, which D10 makes types-only, and it has no effort field — so the value rides `AssistantTurn extends AssistantMessage { effort?: string }` plus `PaneMessage = Exclude<AgentMessage, AssistantMessage> | AssistantTurn`, both in `src/shared/protocol.ts`. Because `effort` is optional the two are mutually assignable, which is what kept the change surgical: the reducer, `AdapterState` and the broadcaster all still say `AgentMessage`, and only the construction site (`mapping.ts:assistant()`) and the render site (`Message.svelte`'s prop) name the wider type. `Exclude` rather than a hand-written union so `CustomAgentMessages` declaration merging still reaches it — `Message.svelte`'s final `{:else}` depends on that openness. `ServerEvent.snapshot.messages`/`upsert.message` were widened too: additive and non-breaking, but D11 makes that file where the wire is stated and the field genuinely rides the payload. Named `effort`, not `reasoningEffort`, because Codex uses both for the same concept (`reasoningEffort` on the responses, `effort` on `ThreadSettings`). One `setIdentity` call in `adapter.ts` covers start and resume because both response types carry `reasoningEffort` identically — OW-8's resume-typed-as-start assertion needed no touching, and the `hydrate` path shares `remap`, so a resumed turn is stamped too. Both present-case tests were re-verified red by the session agent after cherry-pick review, not just reported: `expected [ 'example-model', '52 tok' ] to include 'high'` (`Transcript.svelte.test.ts`, jsdom) and `expected undefined to be 'high'` (`reducer.test.ts`, node). The absent cases pass trivially, so the executor broke the omission logic — stamping `ctx.effort ?? "unknown"` and rendering the span unconditionally — and watched those go red too. `bun run check` green on `main` (634 tests). Not yet seen in a browser against a live Codex turn; the presentation verdict is the owner's. Noted, not changed: `ThreadIdentityResponse` (`reducer.ts:385`) has no references anywhere in `src/`, pre-existing dead code left in place with its `Pick` widened to stay accurate. |
| ~~OW-62~~ | ~~defect~~ | ~~`src/client/render/markdown.ts`, `src/client/render/Markdown.svelte`, `src/client/render/*.test.ts`, `src/server/http/static.ts`~~ | ~~**Repo/local file references in markdown are rendered as ordinary links even though agentpane has nowhere useful to send them.** The sanitizer hook in `markdown.ts` treats every non-`#` anchor as an external link (`target="_blank"`), so references like `src/client/api.ts:35` become clickable, fall into the SPA static handler, and effectively reload the app — a false affordance that also drops context, since the rendered label can collapse to the file name while hiding the line suffix. The agreed fix is deliberately not a file viewer: detect local/repo file references, preserve `path:line` in the visible text, style them distinctly from prose, render them as non-link inline references so text selection keeps working. Real `http(s)` links remain real links.~~ **Fixed** in `721193f`. The row's premise needed one correction, established by probing `marked` 16.4.2 with this module's own options before dispatch: **bare paths in prose are not autolinked** — `see src/client/api.ts:35` renders as plain text — so the defect is only anchors from *explicit* markdown links, and no source-side regex over prose was needed. `[api.ts](src/client/api.ts:35)` was the motivating case: label collapsed, line suffix hidden in the href. Also note the symptom was a *new tab* loading the app, not a reload of the current one, because the hook stamped `target="_blank"`. The fix is a `link` override on the private `Marked` instance beside the existing `code` override, emitting `<span class="ap-file-ref">`; returning `false` hands every other href back to marked's default renderer, so the anchor path to DOMPurify is untouched. Classification is narrow on purpose — no URI scheme at all, or `file:` — because "anything not http(s)" would take `javascript:`/`data:` out of DOMPurify's hands and break `markdown.test.ts`'s "drops javascript: hrefs but keeps the link text". Visible text is the location unless the author's label already contains it, which leaves `[src/a.ts:5](src/a.ts:5)` verbatim and expands a collapsed label. No existing test was edited. The four local-reference cases were re-verified red by the session agent after cherry-pick review, not just reported (`expected <a href="src/client/api.ts:35" ...> to be null`); the two control cases — web URL stays a hardened link, in-document and hostile hrefs stay anchors — cannot go red first by construction and exist to pin non-regression against exactly that widening. `bun run check` green on `main` (640 tests). `src/server/http/static.ts` was in the row's Where and was **not** touched: its SPA fallback is gated on `Accept: text/html` and is correct for a real navigation; the fix removes the navigation instead. Two recorded judgements, neither reversed: the styling (monospace, `--ap-fg-muted`, dotted underline, no accent) is a first cut awaiting the owner's verdict; and a protocol-relative href (`[x](//example.com/y)`) has no scheme, so it is now rendered inert — `//` is a scheme-relative URL by RFC 3986 but also a valid POSIX path, no case has been observed in a transcript, and the failure mode is a link becoming unclickable rather than anything unsafe, so no special case was encoded. Not yet looked at in a browser; the presentation verdict is the owner's. |
| ~~OW-60~~ | ~~defect~~ | ~~`src/client/App.svelte`, `src/client/app.css`, `src/client/render/Message.svelte`, `src/client/App.test.ts`, `e2e/follow.spec.ts`~~ | ~~**The transcript navigation affordance is anchored to the scrolled conversation instead of the pane viewport, and it needs to become a right-rail navigation pill.** Today the only affordance is the `showJumpToLatest` button rendered inside `.conversation` (`App.svelte`, `.jump-to-latest` in `app.css`), so when the reader is mid-history it can scroll off-screen with the content it is supposed to help navigate. Replace it with one always-present, tall narrow pill fixed to the right of the conversation pane, outside the bordered transcript so it never obscures content; localhost-only and owner-only means no responsive/discoverability work is required here. Four stacked segments, icon-only: double-chevron up = start, single-chevron up = previous user input, single-chevron down = next user input, double-chevron down = end. `Prev`/`next` are relative to the first user turn at or below the viewport top, not "most visible"; `start` and `end` mean true top/bottom of the scroller. Grounding for the executor: user turns already render as DOM landmarks with stable `data-index` and `data-role="user"` in `Message.svelte`, and `transcript.ts` preserves those original indices even in reading view specifically so shell scroll logic can keep targeting them. Keep all navigation logic in the shell beside `handleConversationScroll`/`jumpToLatest`; these jumps are manual reading actions, so like a manual scroll they must never arm follow mode. Done when jsdom pins the button-state and target-selection logic (including disabled states at the ends and around consecutive assistant/tool messages), and `bun run test:browser` is extended or replaced with a real-browser check that the right-rail control stays visible while the conversation scrolls and that its jumps land on the intended user turns without regressing follow-mode behavior.~~ **Fixed** in `848d8c2`. The row's grounding was re-checked against the source before dispatch and had not drifted. `Message.svelte` was in the row's Where and needed **no change** — its `data-role="user"`/`data-index` landmarks were already what the rail needed. The rail is a third `auto` grid column of `.shell` (`grid-area: rail`, `align-self: center`), a sibling of `.conversation` rather than a child, which is what makes it immune to all three of the old button's problems at once: it cannot scroll away, cannot sit over the transcript, and cannot count toward the `scrollHeight` the follow-mode math reads. Target selection: the pivot is the first `[data-role="user"]` node whose `anchorTop` is at or below `scrollTop` (4px slack); `prev` is the turn before it, `next` the turn after. Because every jump parks its target's top edge on the viewport top, that target becomes the next pivot, so repeated clicks walk one user turn at a time in either direction. With no pivot (every turn above the viewport top) `prev` is the last turn and `next` is null. `start`/`end` are the scroller's true ends, never the first/last turn. Disabled: `start` = atTop, `prev` = atTop || no earlier turn, `next` = atBottom || no later turn, `end` = atBottom. **Judgement recorded, not reversed:** the `atBottom` half of `next` goes beyond the row's literal wording and is deliberate — the last screenful can hold user turns the scroller can never bring to its top, where `next` would otherwise stay live while clicking it moved nothing; that case has its own jsdom test. All four clear `anchorIndex` and never arm follow; `navigate` also writes back `state.top` after jumping, which the old `jumpToLatest` did not, so a session's remembered scroll position no longer goes stale across a switch. `.conversation` loses `position: relative`, orphaned by deleting the absolutely-positioned button — which also restores what `anchorTop`'s own comment claims it relies on (that neither element is a positioned offsetParent). **One pre-existing defect was found and fixed here because it blocked the rail entirely:** `applyScrollTop` clamped only at zero, so the session-switch effect's `applyScrollTop(el, el.scrollHeight)` — always a full `clientHeight` past the bottom — passed the no-op guard, armed `suppressScrollHandling`, then assigned a value the browser clamped to no change, firing no `scroll` event and stranding the flag, which then swallowed the reader's next genuine scroll. Instrumented in Chromium: after attach, a 4960px scroll fired its event and was discarded while the rail kept reporting the reader was at the top. Now clamped to `scrollHeight - clientHeight`. This forced one existing jsdom expectation from 700 to 200 — that 700 was pinning the jsdom artifact (no layout, so no clamp) that no browser ever holds. Related to but distinct from OW-48, which is about two events against one flag rather than zero events against an armed one. Four new jsdom tests plus one new browser test, every one confirmed failing first: nav candidates widened to all `[data-index]` (`expected +0 to be 1000`), pivot predicate flipped to strictly-below (`expected 2000 to be 1000`), `end` retargeted to the last user turn (`expected 2000 to be 2100`), `start` never disabled, `next` losing its at-bottom condition, a rail jump arming follow (`expected 900 to be 500`), and `prev`/`next` landing 40px off the pane top (`next did not land on user turn 2: 40.05 > 2`). One break initially passed for the wrong reason — an unmocked rect reads 0 in jsdom, which reconciled back to the position the jump had already left behind — and the assertion was strengthened until it went red. The browser test pins the two things jsdom structurally cannot see: the rail is not a descendant of `.conversation`, and its box does not move when the transcript scrolls under it. **The defect itself was reproduced there**: with the rail moved back inside the scroller and the structural assertion bypassed, scrolling moved it from y=9888.9 to y=5124.9 — 4764px, straight off-screen. It also re-checks that a submit still arms follow after the rail has been driven. `e2e/follow.spec.ts` was extended, not replaced; the OW-47 test is untouched. `bun run check` green on `main` (643 tests) and `bun run test:browser` green (2 tests). **Unproven, deliberately:** the `@media (max-width: 42rem)` fallback lays the rail down as a horizontal row between conversation and prompt. Updating that media query was forced — the new grid area has to appear in the single-column `grid-template-areas` or it auto-places — but the result was never rendered at that width, because the row waived responsive work as out of scope for a localhost-only owner-only tool. Not yet looked at in a browser by the owner; the presentation verdict (rail chrome, icon weight, vertical centring) is theirs. |
