---
labels: [unverified, browser-testing]
---

# Toggling Reading view while follow mode is armed has never been exercised against real layout and scroll events.

`e2e/follow.spec.ts`; `e2e/harness.ts`, `runTurn`, `FollowHarness.seed`, `FollowHarness.metrics`; `src/client/App.svelte`, `reading`, `armFollow`, and `reconcile`; `src/client/render/transcript.ts`, `condense`.

OW-51 preserved original transcript indices so condensing cannot strand follow mode's `[data-index]` anchor, and OW-reyale proved in jsdom that Reading view shows, hides, and restores tool and thinking chrome for both live and stored transcripts.
Neither test can settle the interaction this card names because toggling changes the transcript's real height while follow is armed, and jsdom has no layout, scroll anchoring, `ResizeObserver`, or browser scroll-event timing.

The old instruction to wait for OW-24 is obsolete.
The synthetic Playwright harness has grown from OW-47's one-defect probe into the browser UI suite, and this deterministic layout interaction belongs there rather than in the production-server vertical slice.

Extend `e2e/follow.spec.ts` or add one focused sibling spec using the existing contracting turn shape: attach, seed enough completed turns to scroll, submit a paced turn, toggle Reading view on and back off while the turn is streaming, then let the turn settle.
Assert that the tool/thinking chrome actually disappears and returns, the submitted user message remains the same original `[data-index]` anchor, and the final anchor offset remains within the existing `LOCKED_PX` tolerance instead of follow disengaging on either height change.
If the current behavior fails, fix only the follow/reconcile interaction the browser exposes; do not move browser geometry into jsdom or couple this to OW-24's server work.

Done when the browser assertion is shown to discriminate by a targeted sabotage of the relevant follow/reconcile path, passes on the finished behavior, and `bun run check` plus `bun run test:browser` are green.
OW-pezero now carries the separate defect in which a tool-result-only transcript condenses to zero entries and falsely says `No messages yet.`; it is jsdom-visible and is not part of this card.

Added a real-browser Reading view/follow-mode regression in `e2e/follow.spec.ts`, with pre-anchor thinking/tool/result chrome so the original `[data-index]` assertion discriminates, and with visible chrome disappearance/restoration while the turn remains streaming.
The first implementation test exposed a real browser defect without artificial sabotage: the finished anchor landed 247.75px from the pane instead of within the 2px tolerance, reproducing twice in three runs because Chromium's shrink-induced bottom clamp emitted a scroll event that cleared follow.
`src/client/App.svelte` now retains follow only when the event correlates with a recorded `scrollHeight` decrease, the prior follow position lies beyond the new bottom, and Chromium has clamped to that exact bottom; a new one-pixel native-scroll browser test proves genuine fine manual scrolling still disengages follow.
Temporary condense renumbering made the strengthened original-index assertion fail before restoration, and the one-pixel test failed against the broader first guard before the explicit shrink correlation.
Adversarial review found the broader first guard and original fixture insufficient, then marked both focused fixes addressed with spec and quality passing.
Fresh verification on the finished branch passed `bun run check` (47 files, 871 tests, zero TypeScript/Svelte diagnostics) and `bun run test:browser` (10/10).
Landed on main as a69fb3d and 8704fa7.
