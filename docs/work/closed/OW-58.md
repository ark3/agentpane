---
kind: defect
where: '`.claude/skills/execute/SKILL.md:26-28`, OW-43 and OW-45 close notes in `docs/work/closed/`'
---

# Dispatch has no freshness step, and worktrees have twice been cut behind `main`.

OW-45's close note says the worktree was *"cut two commits behind `main`"*,
OW-43's that it was *"cut behind `main`"*; both were handled by re-running `bun
run check` on `main` and explaining the gap in prose, which is bookkeeping
standing in for a fix. **Verify the mechanism before designing one:** the Agent
tool's `isolation: "worktree"` does not document which commit it branches from,
and nobody has checked whether it uses the parent session's start HEAD, the repo
HEAD at spawn, or something else. The answer can invert the fix — if it already
cuts from HEAD at spawn, then the staleness came from elsewhere (the parent
session holding a stale HEAD is the obvious candidate) and adding a rebase at
dispatch would be treating a symptom while the cause keeps producing new ones.
Cheapest measurement: land a commit, then dispatch a throwaway worktree subagent
that reports `git rev-parse HEAD` and nothing else, and compare it to `main`'s
tip. Done when that comparison is recorded in `MANUAL_TESTING.md` as live-run
evidence, and *either* the dispatch step gains an explicit cut-from-current-tip
instruction *or* this row closes on the measurement showing the harness already
does it and naming what actually caused the two stale runs. Lower urgency if
OW-57 lands first, since staleness stops being silent, but not closed by it.
Raised by a second agent reviewing the workflow, 2026-08-16.

**Fixed** in `504e0cb`; the evidence is `MANUAL_TESTING.md`'s "Observed worktree
base for dispatched subagents". **The row's two candidate mechanisms were both
wrong.** It is neither the parent session's start HEAD nor the repo HEAD at
spawn: the harness cuts from the **remote tracking ref**. Two dispatched
worktrees in one session both started at `9ad0a6d`, which is where `origin/main`
and `origin/HEAD` point, while local `main` was at `c04f26c` and then `cda74e6`
— the same base across two runs `main` had moved between is what identifies it,
and the branch reflog states it outright (`branch: Created from origin/main`).
Because `AGENTS.md` forbids `git push` unless asked by name, `origin/main` is
frozen at the last push and the gap grows without bound: 19 commits for the run
that landed `9b786a6`, 21 for the run that recorded this. **That is what caused
the two stale runs OW-43 and OW-45 recorded** — no stale parent HEAD was
involved, and neither close note needs revisiting. Of the row's either/or, the
first branch was taken: dispatch now requires the prompt to tell the subagent to
`git merge --ff-only main` before starting and to report the sha it started at,
so a stale base surfaces in the report instead of in wrong line numbers. Keeping
`origin/main` fresh by pushing would fix it at the source and was rejected — the
never-push rule. **This run exercised step 4's amend**, which OW-57 left
unproven: review found the record argued the base circumstantially from matching
shas when the reflog states the mechanism directly, so the reflog line and the
command that produces it were added on top of the subagent's `5edccfe`, landing
as `504e0cb`. `git worktree list` and `git branch` show only `main`. Docs only,
so `bun run check` does not apply and was not run.
