---
kind: defect
where: '`docs/TRACKING.md` ("Migrating", "Staying greppable", "What was open", the prefix and cost sections), `docs/WORKSTREAMS.md` (OW-54, OW-59), `.claude/skills/execute/SKILL.md`'
---

# OW-54's documentation was not executable as written. Items 1-6 are repaired; two decisions are still the owner's, and they are all that holds this row open.

Found 2026-08-17 by dispatching a cold agent to read OW-54 as if executing it
plus an adversarial audit of the collateral, every finding then confirmed by
hand, and repaired the same day. **Do not re-run the audit and do not re-derive
the findings** — that work is done and paid for; the fixes are in
`docs/TRACKING.md` and OW-54, and the enumeration that used to live in this row
is deleted rather than kept as a second copy of them. What the six repairs were,
one line each, so a reader knows what changed and can go look: (1) the citation
census now covers the repo instead of `docs/` — 65 ids sit in 29 files under
`src/`, `e2e/` and `playwright.config.ts`, 37 files carry one in total, and
`README.md` carries none, so OW-54's old done-when named an empty file and
skipped the larger half. (2) Phase 3 gained a path check listing every site
where prose points at a table phase 3 deletes, and OW-54's Where gained the four
files that hold them. (3) Phase 3 now states a destination for each of the four
things emptying the table orphans, the five-kind legend included. (4) Finding
5's prototype numbers were re-derived against the real corpus and are labelled
simulation where that is what they are. (5) Every number was recounted against
the files rather than trusting the old figure or this row's own first draft,
both of which had drifted. (6) Phase 2 now names the six loaded rules that fight
the migration and what each becomes. **Two corrections to the audit itself, kept
because both were overstated in its favour:** `README.md:64` is a *path*
reference and a real markdown link, but it is not a counterexample to OW-54's
premise that every `OW-` citation is plain text — that premise is true
repo-wide, verified, and simply never covered paths; and TRACKING's `-H` framing
was not wrong so much as incomplete, since reproducing it under ripgrep 15.2.0
showed all three commands print `path:content` when piped and that the prefix is
what `sort -V` orders on. The audit also missed one instance of its own finding
2: four row bodies (`OW-24`, and closed `OW-41`, `OW-53`, `OW-58`) cite the
deleted paths themselves, so the migration's own output carries them. **Both
decisions the audit left open were taken by the owner on 2026-08-17 and are
recorded in `docs/TRACKING.md`, not here.** Item 7: the ~60-line ceiling on the
loaded files is **retired outright rather than moved** — "we spend most of our
tokens on tool calls, not skill file reads" — so no existing rule has to leave
`execute/SKILL.md` to make room for the close procedure and the no-id survey,
and phase 2 writes what the storage needs. Item 8: the phase-1 row tally is
carried in the **phase-1 commit message**, immutable and `git log --grep`-able,
because a recount at a later phase would legitimately differ once the laptop
files a row and the done-when would then fail for the wrong reason; and OW-54
closes as `docs/work/closed/OW-54.md`, under the storage it created, which makes
its own close the first exercise of the new close procedure. **The dry run came
back NOT BLOCKED on 2026-08-17, but it read a version of OW-54 that no longer
exists, so the done-when is not yet honestly met.** A fresh cold agent read the
row as an executor and reported that its first three actions were reads and
counts of things the row named rather than a codebase-wide grep, and that the
only questions it would ask are the two the procedure instructs it to ask
(authorise the fan-out, settle the clone sync). That verdict is worth having. It
also found six defects, and **fixing them in `f335354` changed exactly the parts
an executor depends on** — the frontmatter spec it would hand every fan-out
agent, the phase-3 grep criterion, the id check, the tally clause, and two
additions to the phase-2 and phase-3 site lists. So the text that will actually
be executed has never been read cold. **What remains: one more dry-run read
against the current text, then the close.** Same dispatch as the first, whose
six findings are the argument for paying for a second. The six, for whoever
compares: the frontmatter shape was described but never written out; the phase-3
grep criterion was unsatisfiable, since `TRACKING.md` and two row bodies must
keep the strings it forbade; the id check named `grep -r`, which walks `.git/`;
the tally clause compared against `ls`, which grows if the laptop files a row
between phases; `AGENTS.md:49` would have been left with a dangling antecedent
once `:48` was rewritten; and `WORKSTREAMS.md:15-16` plus
`execute/SKILL.md:15-16` were missing from the phase lists. Two of its claims
were **not** defects and should not be re-fixed: the unpushed commits it flagged
are this repair's own, and its word counts differed from the doc's by counting
method rather than by error. Repairs landed in `71c1b6a`, `97731f0` and
`f335354`; those three have to be pushed before OW-54 runs, per its own sync
precondition.

**Fixed** in `f0e7ef4`, closing the loop rather than running it again: the
second dry run this row asked for did run, in `e82a9a9` and `590ca66`, and it
earned its cost — it found that the conversion spec had never actually been
written out, that the format example added in `f335354` would have produced 52
unparseable files, and that the equivalence check's normalisation list omitted
the three normalisations that exist only on the file side. But the done-when as
written is unreachable by construction, and that is the finding worth keeping:
every dry run's repairs change the parts an executor depends on, which re-arms
this row's own "the text that will actually be executed has never been read
cold". A third read buys a fourth. Closed on that basis (owner, 2026-08-17) with
the remaining verification moved to where it converges — phase 1's own output,
checked mechanically and then spot-checked by one adversarial agent. `f0e7ef4`
also carries what reviewing the prose a third time would not have found:
measuring the spec against all 69 rows showed headline extraction resolves 48 by
bolded lead span and 21 by first sentence with nothing left over, which is what
let the owner reverse the fan-out to a script; and it exposed six rows (OW-38,
OW-39, OW-44, OW-60, OW-61, OW-65) carrying a raw, unescaped pipe character
inside a code span, so splitting a row on every pipe silently truncates the Item
— a corruption neither dry run caught. Two stale claims in the cells above, left
struck rather than edited: "What remains: one more dry-run read" was satisfied
in `e82a9a9`, and the three shas it names as needing to be pushed are no longer
the set — six commits are unpushed, and OW-54's sync precondition covers all of
them.
