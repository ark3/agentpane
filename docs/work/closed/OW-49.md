---
labels: [deferral, browser-testing]
---

# The only test pinning OW-47 never runs unless someone remembers to run it.

`package.json`, `e2e/`

`bun run test:browser` (Playwright, ~30s, headless Chromium) is deliberately
outside `bun run check`, which must stay fast and browser-free — so the
follow-mode pin can rot silently and `bun run check` will stay green while it
does. `AGENTS.md` now says to run it by hand when touching follow-mode scrolling
in `App.svelte` or `.conversation` in `app.css`, which is a convention, not a
gate. Options, none chosen: a second `check:browser` script that CI (if there
ever is one) runs; folding it in behind an env flag; or accepting the convention
and leaving it. Entangled with OW-24 — whoever builds the real browser E2E suite
inherits this question and should settle it there rather than separately.
Surfaced while landing OW-47.

Settled 2026-08-28 after the browser vehicle had grown to eight tests: keep `bun run test:browser` deliberately outside the fast browser-free `bun run check`, and use AGENTS.md's touch rules for local execution. An alias or environment flag would only have hidden Playwright behind `check`; `.github/workflows/ci.yml` runs the two commands as sibling jobs instead (OW-bafeja). Corrected 2026-09-01 (OW-bafeja): the sentence this replaces argued that no gate was possible on a repo with no CI, which was false — `.github/workflows/ci.yml` had been green since 2026-08-12, and this note's original rationale was never the one the evidence supported. The decision is recorded in `docs/DESIGN.md`, "Testing strategy". OW-24 now carries the one missing production-server/scripted-backend vertical slice without reopening this policy.
