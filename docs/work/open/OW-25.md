---
labels: [unverified]
---

# Whether Pi raises approval dialogs when its `trust.json` does *not* already trust the workspace (HANDOFF finding 42 copies one in, so it shows only that the sandboxed path does not *add* a prompt).

**Runs on the home server** as of 2026-09-13, which has `pi 0.85.1`.
This card carried `work-laptop` until then; the run it asks for no longer needs a trip.

Pi approvals
